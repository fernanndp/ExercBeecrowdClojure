(ns projetos-clojure.calculos)

; cálculos básicos
  (println (+ 1 2))
  (println (* 2 3))
  (println (- 0 2))

; verificando se são múltiplos
  (println (= 0 (mod 9 3)))
  (println (= 0 (mod 5 4)))

; verificando se é par
  (println (even? 2))